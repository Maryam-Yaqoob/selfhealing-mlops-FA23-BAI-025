groups:
  - name: model_alerts
    rules:
      - alert: ModelDriftDetected
        expr: sentiment_accuracy_score < 0.7
        for: 10s
        labels:
          severity: critical
        annotations:
          summary: "Model drift detected on sentiment API"
          description: "Model accuracy has dropped below stable threshold."
