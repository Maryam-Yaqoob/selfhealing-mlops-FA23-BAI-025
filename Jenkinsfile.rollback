pipeline {
    agent any

    stages {
        stage('Switch Traffic to Stable (Green)') {
            steps {
                sh '''
                    ssh -o StrictHostKeyChecking=no ubuntu@172.31.34.177 \
                    "kubectl set selector service/sentiment-api-service app=sentiment-api slot=green --overwrite"
                '''
            }
        }
    }
}
