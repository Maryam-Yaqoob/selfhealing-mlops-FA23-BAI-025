groups:
  - name: ml-model-alerts
    rules:
      - alert: ModelConfidenceDrift
        expr: prediction_confidence_score < 0.597
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Model confidence below threshold"
          description: "Confidence dropped below 0.597, triggering rollback."
