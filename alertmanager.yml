global:
  resolve_timeout: 1m

route:
  receiver: "jenkins-rollback-webhook"

receivers:
  - name: "jenkins-rollback-webhook"
    webhook_configs:
      - url: "http://16.16.91.250:8080/generic-webhook-trigger/invoke?token=ROLLBACK_E31CBC_TOKEN"
        send_resolved: false
        http_config:
          basic_auth:
            username: "JENKINS_USERNAME"
            password: "JENKINS_API_TOKEN"
